<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.apartmentsystem.mapper.LeaseContractMapper">

    <insert id="insertLeaseContract" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO lease_contract (house_id, user_id, start_date, end_date, contract_status, monthly_rent, deposit, created_at, updated_at)
        VALUES (#{house_id}, #{user_id}, #{start_date}, #{end_date}, "active", #{monthly_rent}, #{deposit}, now(), now())
    </insert>
    <update id="updateLeaseContractByUserId">
        update lease_contract
        <set>
            <if test="house_id != null">house_id = #{house_id},</if>
            <if test="start_date != null">start_date = #{start_date},</if>
            <if test="end_date != null">end_date = #{end_date},</if>
            <if test="monthly_rent != null">monthly_rent = #{monthly_rent},</if>
            <if test="deposit != null">deposit = #{deposit},</if>
            <if test="contract_status != null">contract_status = #{contract_status},</if>
            updated_at = now()
        </set>
    </update>
    <update id="updateStatusById">
        UPDATE lease_contract
        <set>
            <if test="contract_status != null">contract_status = #{contract_status},</if>
            updated_at = now()
        </set>
        WHERE id = #{id}
    </update>
    <delete id="deleteLeaseContractById">
        DELETE FROM lease_contract WHERE id = #{id}
    </delete>
    <select id="selectLeaseContractByUserName" resultType="com.apartmentsystem.entity.LeaseContract">
        SELECT * FROM lease_contract WHERE user_id = (SELECT id FROM users WHERE user_name = #{user_name})
    </select>
    <select id="selectAllLeaseContract" resultType="com.apartmentsystem.entity.LeaseContract">
        SELECT * FROM lease_contract
    </select>
    <select id="selectActiveLeaseContractByUserId" resultType="com.apartmentsystem.entity.LeaseContract">
        SELECT * FROM lease_contract WHERE user_id = #{user_id} AND contract_status = "active"
    </select>
</mapper>
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.apartmentsystem.mapper.UserMapper">
    <insert id="insertUser">
        INSERT INTO users (username, password, email, phone, role, created_at, updated_at) VALUES (#{username}, #{password}, #{email}, #{phone}, #{role}, NOW(), NOW())
    </insert>
    <insert id="insertNormalUser">
        INSERT INTO users (username, password, email, phone, role, created_at, updated_at, photo, nick_name) VALUES (#{username}, #{password}, #{email}, #{phone}, '1', NOW(), NOW(), "https://www.helloimg.com/i/2025/01/23/6792465d7599b.gif", #{nick_name})
    </insert>
    <insert id="createUser">
        INSERT INTO users (username, password, email, phone, role, created_at, updated_at, photo, nick_name) VALUES (#{username}, #{password}, #{email}, #{phone}, #{role}, NOW(), NOW(), #{photo}, #{nick_name})
    </insert>

    <update id="updateUserPasswordById">
        update users set password = #{password} where id = #{id}
    </update>
    <update id="updateUserById">
        update users set username = #{username}, email = #{email}, phone = #{phone}, role = #{role}, nick_name = #{nick_name}, updated_at = NOW() where id = #{id}
    </update>
    <delete id="deleteUserById">
        delete from users where id = #{id}
    </delete>

    <select id="getUserByUserName" resultType="com.apartmentsystem.entity.User">
        SELECT id,username,role,photo,nick_name FROM users WHERE username = #{username}
    </select>

    <select id="getUserByEmail" resultType="com.apartmentsystem.entity.User">
        SELECT * FROM users WHERE email = #{email}
    </select>

    <select id="getUserByPhone" resultType="com.apartmentsystem.entity.User">
        SELECT * FROM users WHERE phone = #{phone}
    </select>
    <select id="getUserAllByUserName" resultType="com.apartmentsystem.entity.User">
        SELECT * FROM users WHERE username = #{username}
    </select>
    <select id="getAllUser" resultType="com.apartmentsystem.entity.User">
        SELECT id,username,email,phone,role,created_at,updated_at,photo,nick_name FROM users
    </select>

</mapper>
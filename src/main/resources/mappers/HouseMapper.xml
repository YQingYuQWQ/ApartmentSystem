<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.apartmentsystem.mapper.HouseMapper">

    <insert id="insertHouse">
        INSERT INTO house
        <trim prefix="(" suffix=")" suffixOverrides=",">
            house_number, building_name, floor,
            <if test="owner_id != null">owner_id,</if>
            status, created_at, updated_at,
            <if test="water_fee != null">water_fee,</if>
            <if test="power_fee != null">power_fee,</if>
            <if test="price != null">price,</if>
            <if test="area != null">area,</if>
            <if test="images != null">images,</if>
            <if test="images_info != null">images_info,</if>
        </trim>
        VALUES
        <trim prefix="(" suffix=")" suffixOverrides=",">
            #{house_number}, #{building_name}, #{floor},
            <if test="owner_id != null">#{owner_id},</if>
            #{status}, NOW(), NOW(),
            <if test="water_fee != null">#{water_fee},</if>
            <if test="power_fee != null">#{power_fee},</if>
            <if test="price != null">#{price},</if>
            <if test="area != null">#{area},</if>
            <if test="images != null">#{images},</if>
            <if test="images_info != null">#{images_info},</if>
        </trim>
    </insert>
    <update id="updateOwnerByHouseNumber">
        UPDATE house SET owner_id = #{owner_id}, updated_at = NOW() WHERE house_number = #{house_number}
    </update>
    <update id="updateStatusByHouseNumber">
        UPDATE house SET status = #{status}, updated_at = NOW() WHERE house_number = #{house_number}
    </update>
    <update id="updateWaterFeeByHouseNumber">
        UPDATE house SET water_fee = #{water_fee}, updated_at = NOW() WHERE house_number = #{house_number}
    </update>
    <update id="updateElectricityFeeByHouseNumber">
        UPDATE house SET electricity_fee = #{electricity_fee}, updated_at = NOW() WHERE house_number = #{house_number}
    </update>
    <update id="updatePowerFeeByHouseNumber">
        UPDATE house SET power_fee = #{power_fee}, updated_at = NOW() WHERE house_number = #{house_number}
    </update>
    <update id="updateStatusByHouseId">
        UPDATE house SET status = #{status}, updated_at = NOW() WHERE id = #{id}
    </update>
    <update id="updateHouseInfoByHouseNumber">
        UPDATE house SET building_name = #{building_name}, floor = #{floor}, owner_id = #{owner_id}, status = #{status}, price = #{price}, deposit = #{deposit}, area = #{area}, images = #{images}, images_info = #{images_info}, water_fee = #{water_fee}, power_fee = #{power_fee}, updated_at = NOW() WHERE house_number = #{house_number}
    </update>
    <update id="setOwnerNullByHouseNumber">
        UPDATE house SET owner_id = NULL, updated_at = NOW() WHERE house_number = #{house_number}
    </update>
    <delete id="deleteHouseByHouseNumber">
        DELETE FROM house WHERE house_number = #{house_number}
    </delete>
    <select id="getHouseById" resultType="com.apartmentsystem.entity.House">
        SELECT * FROM house WHERE id = #{id}
    </select>
    <select id="getHouseByHouseNumber" resultType="com.apartmentsystem.entity.House">
        SELECT * FROM house WHERE house_number = #{house_number}
    </select>
    <select id="getHouseList" resultType="com.apartmentsystem.entity.House">
        SELECT * FROM house
    </select>
    <select id="showHouseList" resultType="com.apartmentsystem.entity.House">
        SELECT id, house_number, building_name, floor, status, price, area, images, images_info FROM house
    </select>
    <select id="getStatusByHouseNumber" resultType="java.lang.String">
        SELECT status FROM house WHERE house_number = #{house_number}
    </select>
    <select id="getHouseByOwnerId" resultType="com.apartmentsystem.entity.House">
        SELECT * FROM house WHERE owner_id = #{owner_id}
    </select>
    <select id="getHousesByIds" resultType="com.apartmentsystem.entity.House">
        SELECT * FROM house WHERE id IN
        <foreach item="id" collection="list" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

</mapper>